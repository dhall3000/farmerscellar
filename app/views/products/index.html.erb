<h1>Products</h1>

<ul>
  <% @products_for_select.each do |product| %>
    <li><%= link_to product[:name], product_path(product[:id]) %></li>
  <% end %>
</ul>
<%= link_to "New product", new_product_path, class: "btn btn-lg btn-primary outline" %>