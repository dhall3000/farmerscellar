<h1>Products</h1>
<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <%= link_to "Add new product", new_product_path %>
  </div>
</div>

<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <ul>
      <% @products.each do |product| %>
        <li><%= product.name %></li>
      <% end %>
    </ul>
  </div>
</div>