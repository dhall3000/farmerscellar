<h1>BulkBuys#new</h1>
<p>Find me in app/views/bulk_buys/new.html.erb</p>

<% @user_infos.each do |user_info| %>      
  <p>Capturing <%= user_info[:total_amount] %> from user: <%= user_info[:name] %></p>
<% end %>

<p> Total Amount: $<%= @total_bulk_buy_amount %></p>

-------------------------------------------------------

<%= form_tag(bulk_buys_path) do %>
  <% for tote_item in @filled_tote_items %>  
    <%= hidden_field_tag "filled_tote_item_ids[]", tote_item.id %>   
  <% end %>
  <%= submit_tag "Capture payments" %>
<% end %>