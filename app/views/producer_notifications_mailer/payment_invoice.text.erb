
<% @posting_infos.each do |posting, value| %>
---------------------------------------------
Id=<%= posting.id %>,Product=<%= posting.product.name %>,Delivery Date=<%= posting.delivery_date.strftime("%A %b %d, %Y") %>,Units=<%= posting.unit.name %>,Quantity=<%= value[:unit_count] %>,Unit Price=<%= number_to_currency(value[:unit_price]) %>,Sub Total=<%= number_to_currency(value[:sub_total]) %>
<% end %>

Thanks!

Farmer's Cellar
<%= news_url %>